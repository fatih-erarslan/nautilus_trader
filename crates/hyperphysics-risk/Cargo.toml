[package]
name = "hyperphysics-risk"
version = "0.1.0"
edition = "2021"

[dependencies]
hyperphysics-thermo = { path = "../hyperphysics-thermo" }
# hyperphysics-market = { path = "../hyperphysics-market" }  # Temporarily disabled
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
nalgebra.workspace = true

[dev-dependencies]
approx = "0.5"
