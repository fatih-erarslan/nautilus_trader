/**
 * E2B Integration Module
 *
 * Exports the SandboxManager and related utilities for E2B cloud integration.
 *
 * @module e2b
 */

const SandboxManager = require('./sandbox-manager');

module.exports = {
  SandboxManager,
};
