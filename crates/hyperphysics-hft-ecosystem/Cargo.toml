[package]
name = "hyperphysics-hft-ecosystem"
version = "0.1.0"
edition = "2021"
authors = ["HyperPhysics Team <team@hyperphysics.ai>"]
description = "Enterprise-grade HFT ecosystem integrating HyperPhysics, 7 physics engines, and 14+ biomimetic algorithms"
repository = "https://github.com/hyperphysics/hft-ecosystem"
license = "MIT OR Apache-2.0"
keywords = ["hft", "physics-engine", "biomimetic", "trading", "formal-verification"]
categories = ["science", "simulation", "finance"]
rust-version = "1.75.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
# HyperPhysics core crates
hyperphysics-core = { path = "../hyperphysics-core" }
hyperphysics-geometry = { path = "../hyperphysics-geometry" }
hyperphysics-pbit = { path = "../hyperphysics-pbit" }
hyperphysics-thermo = { path = "../hyperphysics-thermo" }
hyperphysics-consciousness = { path = "../hyperphysics-consciousness" }
hyperphysics-market = { path = "../hyperphysics-market", optional = true }
hyperphysics-risk = { path = "../hyperphysics-risk", optional = true }
active-inference-agent = { path = "../active-inference-agent" }

# Lorentz/Hyperboloid model for hyperbolic similarity (optional)
hyperphysics-lorentz = { path = "../hyperphysics-lorentz", optional = true }

# pBit-aware MoE strategy router for unified strategy selection
hyperphysics-strategy-router = { path = "../hyperphysics-strategy-router", optional = true }

# REAL optimization algorithms (NOT stubs)
hyperphysics-optimization = { path = "../hyperphysics-optimization", optional = true }

# Neural forecasting integration
hyperphysics-neural-trader = { path = "../hyperphysics-neural-trader", optional = true }

# Quantum-inspired ML forecasting (hyperphysics-ml)
hyperphysics-ml = { path = "../hyperphysics-ml", optional = true, features = ["cpu"] }

# Physics engines (all optional, for modular deployment)
rapier-hyperphysics = { path = "../physics-engines/rapier-hyperphysics", optional = true }
jolt-hyperphysics = { path = "../physics-engines/jolt-hyperphysics", optional = true }
warp-hyperphysics = { path = "../physics-engines/warp-hyperphysics", optional = true }

# Trading systems (optional until we fix ats-core compilation issues)
ats-core = { path = "../ats-core", optional = true }
# cwts-core = { path = "../cwts-core", optional = true }
# cwts-intelligence = { path = "../cwts-intelligence", optional = true }
# cwts-ultra = { path = "../cwts-ultra", optional = true }

# Bio-inspired workspace (DEPRECATED - use hyperphysics-optimization instead)
# These are thin interface stubs, kept for backwards compatibility
whale-optimization = { path = "../bio-inspired-workspace/whale-optimization", optional = true }
cuckoo-search = { path = "../bio-inspired-workspace/cuckoo-search", optional = true }
bat-algorithm = { path = "../bio-inspired-workspace/bat-algorithm", optional = true }
firefly-algorithm = { path = "../bio-inspired-workspace/firefly-algorithm", optional = true }
dynamic-swarm-selector = { path = "../bio-inspired-workspace/dynamic-swarm-selector", optional = true }
market-regime-detector = { path = "../bio-inspired-workspace/market-regime-detector", optional = true }

# Core dependencies
tokio = { version = "1.40", features = ["full", "macros", "rt-multi-thread"] }
async-trait = "0.1"
futures = "0.3"
rayon = "1.10"

# Math and linear algebra
nalgebra.workspace = true
ndarray = { version = "0.15", features = ["rayon", "serde"] }
num-traits = "0.2"
num-complex = { version = "0.4", features = ["serde"] }
statrs = "0.17"

# Concurrency
dashmap = "6.1"
parking_lot = "0.12"
crossbeam = "0.8"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1.3"

# Error handling
thiserror = "1.0"
anyhow = "1.0"

# Logging and tracing
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Time
chrono = { version = "0.4", features = ["serde"] }

# Random
rand = { version = "0.8", features = ["small_rng"] }
rand_distr = "0.4"

# System info
num_cpus = "1.16"

# SIMD (for performance-critical paths)
wide = { version = "0.7", optional = true }

# FFI for physics engines - Updated for CVE RUSTSEC-2025-0020 fix
pyo3 = { version = "0.24", optional = true, features = ["auto-initialize"] }

# WASM support
wasm-bindgen = { version = "0.2", optional = true }
js-sys = { version = "0.3", optional = true }
web-sys = { version = "0.3", optional = true }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
proptest = "1.5"
approx = "0.5"
tokio-test = "0.4"

[features]
default = ["hft-core", "optimization-real", "simd"]

# Core features
hft-core = ["hyperphysics-market", "hyperphysics-risk"]
simd = ["wide"]

# REAL optimization algorithms via hyperphysics-optimization
# This is the PRIMARY way to use biomimetic algorithms
optimization-real = ["hyperphysics-optimization"]

# Neural forecasting via hyperphysics-neural-trader
neural-forecasting = ["hyperphysics-neural-trader"]

# Quantum-inspired ML forecasting via hyperphysics-ml
quantum-forecasting = ["hyperphysics-ml"]

# DEPRECATED: Legacy bio-inspired-workspace stubs (use optimization-real instead)
biomimetic-tier1 = [
    "whale-optimization",
    "bat-algorithm",
    "firefly-algorithm",
    "cuckoo-search"
]

# Physics engine features (modular selection)
physics-rapier = ["rapier-hyperphysics"]
physics-jolt = ["jolt-hyperphysics"]
physics-warp = ["warp-hyperphysics"]
physics-all = ["physics-rapier", "physics-jolt", "physics-warp"]

# Language binding features
python-bindings = ["pyo3"]
wasm-bindings = ["wasm-bindgen", "js-sys", "web-sys"]

# Formal verification
verification = []

# Lorentz model for hyperbolic similarity metrics
lorentz = ["hyperphysics-lorentz", "hyperphysics-geometry/lorentz"]

# pBit-aware MoE strategy routing for unified strategy selection
strategy-routing = ["hyperphysics-strategy-router"]

# Full hyperbolic strategy system (Lorentz + MoE routing)
hyperbolic-strategy = ["lorentz", "strategy-routing"]

# Full feature set (for testing)
full = [
    "hft-core",
    "optimization-real",
    "neural-forecasting",
    "quantum-forecasting",
    "physics-all",
    "simd",
    "verification",
    "hyperbolic-strategy"
]

[[bench]]
name = "latency_benchmarks"
harness = false

[package.metadata.docs.rs]
all-features = true
targets = ["x86_64-unknown-linux-gnu", "x86_64-apple-darwin"]
