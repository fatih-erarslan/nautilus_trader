[package]
name = "iqad"
version = "1.0.0"
edition = "2021"
authors = ["CQGS Quantum Sentinels"]
description = "Enterprise-grade Immune-inspired Quantum Anomaly Detection with real quantum computing"
license = "MIT OR Apache-2.0"
keywords = ["quantum", "anomaly-detection", "immune-system", "enterprise", "simd"]
categories = ["science", "algorithms", "mathematics"]

[[bin]]
name = "iqad"
path = "src/main.rs"

# Standalone crate
[workspace]

[dependencies]
# pBit-based quantum computing (primary)
quantum-core = { path = "../quantum-core" }

# Legacy quantum computing (optional)
roqoqo = { version = "1.13", optional = true }
roqoqo-quest = { version = "0.13", optional = true }
qoqo_calculator = { version = "0.8", optional = true }
struqture = { version = "1.5", optional = true }

# Linear Algebra & Math
nalgebra = { version = "0.33", features = ["serde-serialize", "rayon"] }
ndarray = { version = "0.16", features = ["rayon", "blas"] }
num-complex = "0.4"
num-traits = "0.2"
statrs = "0.16"
rand = "0.8"
rand_distr = "0.4"
special = "0.10"  # Special mathematical functions

# SIMD & Performance
# packed_simd_2 = "0.3"  # Requires nightly Rust
# simdeez requires nightly for now
wide = "0.7"  # Stable SIMD operations
multiversion = { version = "0.7", optional = true }  # Multiple SIMD implementations

# GPU Acceleration
cuda-sys = { version = "0.2", optional = true }
opencl3 = { version = "0.9", optional = true }
wgpu = { version = "0.19", optional = true }
candle-core = { version = "0.9", optional = true, features = ["cuda"] }

# Async & Concurrency
tokio = { version = "1.35", features = ["full"] }
async-trait = "0.1"
futures = "0.3"
rayon = "1.8"
crossbeam = "0.8"
parking_lot = "0.12"
dashmap = "5.5"

# Memory & Caching
lru = "0.12"
moka = { version = "0.12", features = ["future"] }
cached = "0.48"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1.3"
rmp-serde = "1.1"

# Error Handling & Logging
thiserror = "1.0"
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Hardware Detection
raw-cpuid = "11.0"
sysinfo = "0.30"
num_cpus = "1.16"

# Monitoring & Metrics
prometheus = { version = "0.13", optional = true }
metrics = "0.22"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports", "async_tokio"] }
proptest = "1.4"
quickcheck = "1.0"
approx = "0.5"
pretty_assertions = "1.4"

[features]
default = ["simd", "gpu-wgpu"]
simd = []
gpu-cuda = ["cuda-sys", "candle-core/cuda"]
gpu-opencl = ["opencl3"]
gpu-wgpu = ["wgpu"]
monitoring = ["prometheus"]
# Legacy roqoqo support (optional - use pBit by default)
roqoqo = ["dep:roqoqo", "dep:roqoqo-quest", "dep:qoqo_calculator", "dep:struqture"]
# python-bindings = ["pyo3"]  # disabled for compilation test

# Python bindings (optional) - temporarily disabled for compilation test
# [dependencies.pyo3]
# version = "0.20"
# features = ["extension-module", "abi3-py38"]
# optional = true

[target.'cfg(target_arch = "x86_64")'.dependencies]
# x86_64 specific optimizations

[target.'cfg(target_arch = "aarch64")'.dependencies]
# ARM64 specific optimizations


[[bench]]
name = "performance_benchmarks"
harness = false