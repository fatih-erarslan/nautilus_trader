//! GPU kernel implementations

pub struct GPUKernel;

impl GPUKernel {
    pub fn new() -> Self {
        Self
    }
}

impl Default for GPUKernel {
    fn default() -> Self {
        Self::new()
    }
}